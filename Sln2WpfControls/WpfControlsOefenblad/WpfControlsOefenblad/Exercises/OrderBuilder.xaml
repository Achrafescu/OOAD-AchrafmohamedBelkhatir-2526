<Page x:Class="WpfControlsOefenblad.Exercises.OrderBuilder"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfControlsOefenblad.Exercises"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderBuilder">

    <Grid Background="#eeeeee">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="White">
            <!-- HIERONDER BEGINT XAML VAN DE OEFENING -->
            <Grid Margin="40,25,40,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
               
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="20,0,0,0">
                    <TextBlock Text="Extras" VerticalAlignment="Top" HorizontalAlignment="Left" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <CheckBox x:Name="chbKaas" Content="Kaas" IsChecked="True" Margin="0,0,0,5">
                    </CheckBox>
                    <CheckBox x:Name="chbSpek" Content="Spek" IsChecked="False" Margin="0,0,0,5">
                    </CheckBox>
                    <CheckBox x:Name="chbExtraSaus" Content="Extra Saus" IsChecked="True" Margin="0,0,0,5">
                    </CheckBox>
                    <CheckBox x:Name="chbUi" Content="Ui" IsChecked="False" Margin="0,0,0,5">
                    </CheckBox>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="20,0,0,0">
                    <TextBlock Text="Levering" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <RadioButton x:Name="rbnAfhalen" Content="Afhalen" GroupName="group1" Margin="0,0,0,5"/>
                    <RadioButton x:Name="rbnLevering" Content="Levering" GroupName="group1" Margin="0,0,0,5"/>
                    <RadioButton x:Name="rbnTerPlaatse" Content="Ter plaatse" GroupName="group1" Margin="0,0,0,5"/>

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="btnBevestig" Content="Bevestig" HorizontalAlignment="Left" Margin="0,20, 20,0" Padding="10,5" VerticalAlignment="Bottom" Click="btnBevestig_Click"/>
                        <Button x:Name="btnReset" Content="Reset" HorizontalAlignment="Left" Margin="10,10,0,0" Padding="10,5" VerticalAlignment="Bottom" Click="btnReset_Click"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="20,0,0,0">
                    <TextBlock Text="Samenvatting" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="tbSamenvatting" Text="..." FontSize="14" FontWeight="Normal" Margin="0,0,0,10"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Page>
